import { router } from 'expo-router'; import { StyleSheet, Text, View } from 'react-native'; import { Card, Header, Pill, Screen } from '@/components/ui'; import { subjects } from '@/data/mock'; import { colors } from '@/constants/theme';
export default function Page(){return <Screen><Header title="Subjects and modules" subtitle="Senior 3 · Term 2" back onBack={()=>router.back()}/>subjects.map(x=><Card key={x.name} style={s.card}><Text style={s.title}>{x.name}</Text><Text style={s.sub}>{x.teacher}</Text><Text style={s.link}>Open module →</Text></Card>)</Screen>}
const s=StyleSheet.create({card:{marginBottom:9},row:{flexDirection:'row',gap:18,alignItems:'center',marginBottom:9},time:{fontSize:18,fontWeight:'900',color:colors.red},title:{fontSize:16,fontWeight:'900',marginTop:10},sub:{color:colors.muted,marginTop:5},link:{color:colors.red,fontWeight:'900',marginTop:14}})
