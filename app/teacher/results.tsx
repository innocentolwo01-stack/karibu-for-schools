import { useState } from 'react'; import { router } from 'expo-router'; import { StyleSheet, Text, TextInput, View } from 'react-native'; import { Button, Card, Header, Screen } from '@/components/ui'; import { teacherStudents } from '@/data/mock'; import { colors } from '@/constants/theme';
export default function EnterResults(){const [scores,setScores]=useState<Record<string,string>>({});const [saved,setSaved]=useState(false);return <Screen><Header title="Enter results" subtitle="S3 Biology · Cell structure test" back onBack={()=>router.back()}/>{saved?<Card style={s.saved}><Text style={s.savedText}>✓ Results saved as draft</Text></Card>:null}{teacherStudents.map(x=><Card key={x} style={s.row}><Text style={s.name}>{x}</Text><TextInput value={scores[x]??''} onChangeText={v=>setScores({...scores,[x]:v.replace(/[^0-9]/g,'').slice(0,3)})} style={s.input} keyboardType="number-pad" placeholder="0"/><Text style={s.out}>/100</Text></Card>)}<Button label="Save results" variant="black" onPress={()=>setSaved(true)}/></Screen>}
const s=StyleSheet.create({saved:{backgroundColor:colors.paleGreen,marginBottom:10},savedText:{color:colors.green,fontWeight:'900'},row:{flexDirection:'row',alignItems:'center',marginBottom:8},name:{flex:1,fontWeight:'900'},input:{width:60,height:44,borderWidth:1,borderColor:colors.border,borderRadius:12,textAlign:'center',fontWeight:'900'},out:{color:colors.muted,marginLeft:6}})
