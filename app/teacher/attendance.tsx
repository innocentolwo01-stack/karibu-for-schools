import { useState } from 'react'; import { router } from 'expo-router'; import { StyleSheet, Switch, Text, View } from 'react-native'; import { Button, Card, Header, Screen } from '@/components/ui'; import { teacherStudents } from '@/data/mock'; import { colors } from '@/constants/theme';
export default function Register(){const [present,setPresent]=useState<Record<string,boolean>>(Object.fromEntries(teacherStudents.map(x=>[x,true])));const [saved,setSaved]=useState(false);return <Screen><Header title="S3 Biology register" subtitle="Tuesday, 4 August · 36 students" back onBack={()=>router.back()}/>{saved?<Card style={s.saved}><Text style={s.savedText}>✓ Register saved successfully</Text></Card>:null}{teacherStudents.map(x=><Card key={x} style={s.row}><View style={{flex:1}}><Text style={s.name}>{x}</Text><Text style={s.sub}>{present[x]?'Present':'Absent'}</Text></View><Switch value={present[x]} onValueChange={v=>setPresent({...present,[x]:v})} trackColor={{true:colors.green}}/></Card>)}<Button label="Save register" onPress={()=>setSaved(true)}/></Screen>}
const s=StyleSheet.create({saved:{backgroundColor:colors.paleGreen,marginBottom:10},savedText:{color:colors.green,fontWeight:'900'},row:{flexDirection:'row',alignItems:'center',marginBottom:8},name:{fontWeight:'900'},sub:{color:colors.muted,marginTop:4}})
